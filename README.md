potential-octo-sansa
====================

Dot Files

Usage
##

clone the repo and link up the files
```
# CD into repo!
ln -s $PWD/.vimrc ~/.vimrc
ln -s $PWD/.bashrc ~/.bashrc
ln -s $PWD/.zshrc ~/.zshrc
```


